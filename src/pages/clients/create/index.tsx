import { Grid } from "@mui/material";
import FormCreateClient from "../../../views/clients/create";

const CreateClient = () => {
  return (
    <Grid item xs={12} md={12}>
      <FormCreateClient />
    </Grid>
  );
};

export default CreateClient;
